import{_ as a,o as s,c as n,a0 as d}from"./chunks/framework.DoRPXVp_.js";const g=JSON.parse('{"title":"出口节点：路由、DNS 与分流","description":"","frontmatter":{"title":"出口节点：路由、DNS 与分流"},"headers":[],"relativePath":"design/exit-node-routing-and-dns.md","filePath":"design/exit-node-routing-and-dns.md"}'),e={name:"design/exit-node-routing-and-dns.md"};function i(o,t,l,p,r,c){return s(),n("div",null,[...t[0]||(t[0]=[d(`<h1 id="出口节点-路由、dns-与分流" tabindex="-1">出口节点：路由、DNS 与分流 <a class="header-anchor" href="#出口节点-路由、dns-与分流" aria-label="Permalink to &quot;出口节点：路由、DNS 与分流&quot;">​</a></h1><blockquote><p>说明性文档 · 2026-09-24 配套设计：<code>docs/superpowers/specs/2026-09-23-exit-node-dataplane-design.md</code>（提供端转发）</p><p>文中的结论分三档：<strong>【实测】</strong> 在本项目环境里亲手验证过；<strong>【代码】</strong> 读代码得出；<strong>【推断】</strong> 合理判断但没有验证。</p></blockquote><h2 id="_1-一句话" tabindex="-1">1. 一句话 <a class="header-anchor" href="#_1-一句话" aria-label="Permalink to &quot;1. 一句话&quot;">​</a></h2><p>出口节点 = <strong>一条 WireGuard 三层隧道</strong> + <strong>出口机器上的 NAT</strong>。</p><p>隧道只搬运 <strong>IP 包</strong>：它不认识 HTTP，不认识域名，也不知道你在访问 Google 还是百度。 这一点决定了本文里所有的设计取舍，也是它和 Clash 最大的区别（见第 6 节）。</p><h2 id="_2-一个-https-请求的完整旅程" tabindex="-1">2. 一个 HTTPS 请求的完整旅程 <a class="header-anchor" href="#_2-一个-https-请求的完整旅程" aria-label="Permalink to &quot;2. 一个 HTTPS 请求的完整旅程&quot;">​</a></h2><p>以手机 Chrome 访问 <code>https://www.google.com</code> 为例，出口模式开启：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>① DNS：先把 www.google.com 解析成 IP                 ← 第 3.2 节专门讲</span></span>
<span class="line"><span>② Chrome 对这个 IP:443 发起 TCP + TLS，发 HTTP 请求</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>手机系统 TCP/IP 栈：把请求切成 IP 包</span></span>
<span class="line"><span>      [IP头 src=10.96.0.4 dst=142.250.x.x | TCP | TLS 加密后的 HTTP 数据]</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>路由表：dst 落在 0.0.0.0/1 或 128.0.0.0/1（出口模式装的两条路由）</span></span>
<span class="line"><span>      → 交给虚拟网卡 utun，而不是 Wi-Fi / 蜂窝</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>隧道扩展（Network Extension）读出这个原始 IP 包 → 交给 Go 引擎</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>WireGuard：把「整个 IP 包」加密，装进一个 UDP 包</span></span>
<span class="line"><span>      [外层 IP：手机公网 → 出口公网 | UDP | WireGuard 密文]</span></span>
<span class="line"><span>      │  经过 Wi-Fi/蜂窝，穿过互联网（ICE 直连，或走 relay 中继）</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>出口机器（香港）：WireGuard 解密，还原出内层 IP 包</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>出口内核：① FORWARD 放行  ② MASQUERADE：把源 10.96.0.4 改成出口自己的公网 IP</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>发往真正的 Google（对方看到的源是香港 IP）</span></span>
<span class="line"><span>      │</span></span>
<span class="line"><span>      ▼</span></span>
<span class="line"><span>回程：Google → 出口 → 内核按连接跟踪把目的地址改回 10.96.0.4</span></span>
<span class="line"><span>      → WireGuard 加密 → 发回手机 → 解密 → 写回 utun → 系统栈 → Chrome</span></span></code></pre></div><p><strong>HTTP 的数据是不是都进了隧道？</strong> 是的，但准确地说是：<strong>所有匹配路由的 IPv4 包</strong>（TCP、UDP、ICMP，包括 DNS 和 QUIC）都进隧道，HTTP 只是 TCP 载荷。</p><p>两层加密的关系：</p><table tabindex="0"><thead><tr><th>层</th><th>谁和谁之间</th><th>防谁</th></tr></thead><tbody><tr><td>TLS（HTTPS 自带）</td><td>Chrome ↔ Google</td><td>出口节点也看不到 HTTPS 内容</td></tr><tr><td>WireGuard</td><td>手机 ↔ 出口</td><td>中间网络看不到你在访问什么</td></tr></tbody></table><p>出口能看到：目的 IP、端口、流量大小。<strong>看不到 HTTPS 的内容</strong>（明文 HTTP 除外）。</p><h3 id="哪些流量故意不进隧道" tabindex="-1">哪些流量<strong>故意不进</strong>隧道 <a class="header-anchor" href="#哪些流量故意不进隧道" aria-label="Permalink to &quot;哪些流量**故意不进**隧道&quot;">​</a></h3><p>这些是排除项，不排除会&quot;自己吃自己&quot;：</p><ol><li><strong>控制面地址</strong>（如 <code>101.36.119.12</code>）：NE 设置里把它列为排除路由，否则手机连不上控制面，也就收不到&quot;取消选择&quot;的推送。【代码】<code>PacketTunnelProvider.swift</code> 的 <code>excluded.append(hostIP)</code>。</li><li><strong>隧道扩展自己发出的包</strong>（WireGuard 的 UDP、中继的 TCP）：如果它们也被路由回隧道，就会&quot;外层包被当作内层包再加密一次&quot;，无限循环。Mac 版用「socket 绑定物理网卡」（<code>bind interface: en0</code>）避免；<strong>iOS 版目前没有这一步</strong>。【代码】</li><li><strong>局域网网段</strong>：有更具体的路由，不进隧道。</li><li><strong>IPv6</strong>：目前<strong>没有接管</strong>，有 IPv6 的网站可能绕过出口直连。【代码】这是已知缺口。</li></ol><h2 id="_3-三件事要分开想" tabindex="-1">3. 三件事要分开想 <a class="header-anchor" href="#_3-三件事要分开想" aria-label="Permalink to &quot;3. 三件事要分开想&quot;">​</a></h2><p>出口节点其实同时在做三件互相独立的事，很多困惑来自把它们混在一起：</p><table tabindex="0"><thead><tr><th>层</th><th>问题</th><th>由谁决定</th></tr></thead><tbody><tr><td><strong>路由</strong></td><td>哪些包进隧道？</td><td>手机的路由表（<code>includedRoutes</code> / <code>excludedRoutes</code>）</td></tr><tr><td><strong>DNS</strong></td><td>域名怎么变成 IP？</td><td>手机的系统 DNS 设置</td></tr><tr><td><strong>NAT</strong></td><td>包到了出口怎么出去？</td><td>出口机器的 iptables</td></tr></tbody></table><h3 id="_3-1-路由" tabindex="-1">3.1 路由 <a class="header-anchor" href="#_3-1-路由" aria-label="Permalink to &quot;3.1 路由&quot;">​</a></h3><p>出口模式下，NE 设置里的路由是：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>included:  10.96.0.0/24      overlay 网段（始终在隧道里）</span></span>
<span class="line"><span>           0.0.0.0/1         ┐ 合起来就是 0.0.0.0/0</span></span>
<span class="line"><span>           128.0.0.0/1       ┘ 拆成两半，比系统默认路由更具体，所以必胜</span></span>
<span class="line"><span>excluded:  控制面 IP/32</span></span></code></pre></div><p>为什么不直接写 <code>0.0.0.0/0</code>？因为单个 <code>/0</code> 会被系统降级，抢不过物理网卡的默认路由。WireGuard 官方客户端也是这么做的。【代码】</p><h3 id="_3-2-dns" tabindex="-1">3.2 DNS <a class="header-anchor" href="#_3-2-dns" aria-label="Permalink to &quot;3.2 DNS&quot;">​</a></h3><p><strong>为什么必须单独处理？</strong> 隧道不认识域名。手机要先知道 <code>www.google.com</code> 的 IP，才有后面的包。</p><p><strong>如果不处理会怎样？</strong>【实测】手机用运营商的 DNS，google.com 被污染成一个<strong>假的 Facebook 地址</strong>（<code>31.13.92.37</code>、<code>157.240.7.20</code>）。抓包里能看到手机对这两个地址反复重试上百次 SYN，永远没有回应。这是典型的 DNS 污染特征。</p><p><strong>关键概念：DNS 设置只决定&quot;往哪发&quot;，进不进隧道由路由决定。</strong></p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>系统 DNS 设置（NEDNSSettings）  →  &quot;解析域名时，把查询发给 8.8.8.8&quot;</span></span>
<span class="line"><span>                                        │</span></span>
<span class="line"><span>                                        ▼   系统发出一个普通 UDP 包：</span></span>
<span class="line"><span>                                        │   10.96.0.4:随机端口 → 8.8.8.8:53</span></span>
<span class="line"><span>                                        ▼</span></span>
<span class="line"><span>                                  查路由表：8.8.8.8 落在 0.0.0.0/1</span></span>
<span class="line"><span>                                        │</span></span>
<span class="line"><span>                                        ▼</span></span>
<span class="line"><span>                                  交给 utun → 进隧道 → 出口 → 8.8.8.8</span></span></code></pre></div><p>所以&quot;DNS 进了隧道&quot;不是 DNS 设置的魔法，纯粹因为 <strong>DNS 服务器的地址落在了隧道的路由范围里</strong>。这是 3.2 节之后所有方案的核心。</p><p><strong><code>matchDomains</code> 的坑</strong>【实测】：</p><table tabindex="0"><thead><tr><th>写法</th><th>效果</th></tr></thead><tbody><tr><td><code>matchDomains = nil</code></td><td><strong>不会</strong>让隧道 DNS 成为默认解析器，域名照旧走运营商 DNS（Mac 上 <code>scutil --dns</code> 仍显示物理 DNS <code>114.114.114.114</code>）</td></tr><tr><td><code>matchDomains = [&quot;&quot;]</code></td><td>隧道 DNS 成为<strong>默认</strong>解析器，所有域名都走它（WireGuard 官方同款）</td></tr></tbody></table><h3 id="_3-3-nat-出口机器" tabindex="-1">3.3 NAT（出口机器） <a class="header-anchor" href="#_3-3-nat-出口机器" aria-label="Permalink to &quot;3.3 NAT（出口机器）&quot;">​</a></h3><p>出口 agent 在检测到自己宣告了路由后，幂等地装这几条（<code>internal/agent/provision/exit_gateway.go</code>）：【代码】</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>sysctl net.ipv4.ip_forward=1</span></span>
<span class="line"><span>iptables -A FORWARD -i wf0 -j ACCEPT                       # 隧道进来的放行</span></span>
<span class="line"><span>iptables -A FORWARD -o wf0 -m conntrack --ctstate ESTABLISHED,RELATED -j ACCEPT   # 回程放行</span></span>
<span class="line"><span>iptables -t nat -A POSTROUTING -o &lt;默认出网卡&gt; -s 10.96.0.0/24 -j MASQUERADE      # 源地址改写</span></span></code></pre></div><h2 id="_4-dns-的几种做法" tabindex="-1">4. DNS 的几种做法 <a class="header-anchor" href="#_4-dns-的几种做法" aria-label="Permalink to &quot;4. DNS 的几种做法&quot;">​</a></h2><h3 id="_4-1-云主机-有-dns-是什么意思" tabindex="-1">4.1 云主机&quot;有 DNS&quot;是什么意思 <a class="header-anchor" href="#_4-1-云主机-有-dns-是什么意思" aria-label="Permalink to &quot;4.1 云主机&quot;有 DNS&quot;是什么意思&quot;">​</a></h3><p>先澄清一个容易混淆的点。【实测】你的出口机器：</p><table tabindex="0"><thead><tr><th>检查</th><th>结果</th></tr></thead><tbody><tr><td><code>/etc/resolv.conf</code></td><td><code>10.8.255.1</code>、<code>10.8.255.2</code>（云厂商内网 DNS），备用 <code>114.114.114.114</code></td></tr><tr><td>本机解析 <code>www.google.com</code></td><td>返回真实 Google 香港地址，无污染</td></tr><tr><td>端口 53 有没有进程在监听</td><td><strong>没有</strong></td></tr></tbody></table><ul><li>&quot;有 DNS&quot;指它<strong>有上游可以问</strong>（<code>resolv.conf</code> 是客户端配置）；</li><li>它<strong>没有</strong>一个在监听 53 端口、能回答别人查询的 <strong>DNS 服务</strong>。</li></ul><h3 id="_4-2-方案-a-把公共-dns-设成系统-dns-当前方案" tabindex="-1">4.2 方案 A：把公共 DNS 设成系统 DNS（当前方案） <a class="header-anchor" href="#_4-2-方案-a-把公共-dns-设成系统-dns-当前方案" aria-label="Permalink to &quot;4.2 方案 A：把公共 DNS 设成系统 DNS（当前方案）&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>手机 ──(隧道)──▶ 出口(只做转发+NAT) ──▶ 8.8.8.8（互联网上的公共 DNS）</span></span>
<span class="line"><span>                    ▲                        真正回答的是它</span></span>
<span class="line"><span>                 不需要 DNS 服务</span></span></code></pre></div><ul><li>系统 DNS = <code>8.8.8.8</code>、<code>1.1.1.1</code>，<code>matchDomains = [&quot;&quot;]</code>。</li><li>出口<strong>只是路由器</strong>，回答问题的是互联网上的 8.8.8.8。<strong>出口上不需要任何 DNS 服务。</strong></li><li>引擎不参与：DNS 包和其他任何包一视同仁。</li><li>代价：出口模式期间 <code>*.lattice</code> 短名解析不了（用 overlay IP 直连不受影响）。</li><li>状态：【实测】在 iPhone 上，Chrome 通过出口访问到了 Google 的真实服务器（<code>142.250.x.x</code>/<code>142.251.x.x</code> 完成握手，QUIC 在走）。 <strong>注意</strong>：那次抓包里没抓到 UDP 53（Chrome 可能用了自己的加密 DNS 或命中缓存），所以&quot;DNS 包进了隧道&quot;没有被直接抓到，只能从&quot;连上的是真实 Google 地址&quot;间接说明解析是干净的。</li></ul><h3 id="_4-3-方案-b-出口节点自带-dns-转发器" tabindex="-1">4.3 方案 B：出口节点自带 DNS 转发器 <a class="header-anchor" href="#_4-3-方案-b-出口节点自带-dns-转发器" aria-label="Permalink to &quot;4.3 方案 B：出口节点自带 DNS 转发器&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>手机 ──▶ 10.96.0.8:53（出口的 overlay 地址） ──▶ 出口把查询转给 10.8.255.1（云内网 DNS）</span></span>
<span class="line"><span>          走 overlay 路由，永远在隧道里</span></span></code></pre></div><ul><li>系统 DNS = 出口的 overlay 地址。出口 agent 新增一个监听 <code>10.96.0.8:53</code> 的小转发器。</li><li><strong>不依赖全局路由</strong>：overlay 路由 <code>10.96.0.0/24</code> 永远存在。</li><li>上游是云厂商内网 DNS（【实测】从 overlay 地址 <code>10.96.0.8</code> 去问能通），比公网 8.8.8.8 更近。</li><li>可以顺手做缓存、日志、回答 <code>*.lattice</code>。</li><li>代价：出口要多跑一个组件；<strong>出口离线时 DNS 也跟着失效</strong>（A 反而更稳：出口离线时服务端撤掉 <code>0.0.0.0/0</code>，DNS 自然回到手机自己的网络）。</li></ul><h3 id="_4-4-方案-c-引擎做包级-dnat" tabindex="-1">4.4 方案 C：引擎做包级 DNAT <a class="header-anchor" href="#_4-4-方案-c-引擎做包级-dnat" aria-label="Permalink to &quot;4.4 方案 C：引擎做包级 DNAT&quot;">​</a></h3><ul><li>系统 DNS 保持虚拟地址 <code>10.96.0.1</code>；引擎收到发往它的非 lattice 查询，<strong>只改目的地址</strong>成真实解析器后原样转发，回程再把源地址改回。</li><li>全程是&quot;改地址&quot;，不自己开 socket、不合成应答。</li><li>代价：引擎要维护改写状态表，客户端变复杂。</li></ul><h3 id="_4-5-方案-d-fake-ip-clash-的做法" tabindex="-1">4.5 方案 D：fake-ip（Clash 的做法） <a class="header-anchor" href="#_4-5-方案-d-fake-ip-clash-的做法" aria-label="Permalink to &quot;4.5 方案 D：fake-ip（Clash 的做法）&quot;">​</a></h3><ul><li>本地 DNS 直接返回假地址 <code>198.18.x.x</code>，等应用连过来，再反查出它想访问的域名，交给远端解析。</li><li>优点：能<strong>按域名</strong>分流。</li><li>前提：必须能在用户态还原出 TCP 连接（需要内置协议栈）。这是仓库里 <code>2026-09-23-gvisor-embedded-networking-design.md</code> 那条线，是大改造。</li></ul><h3 id="_4-6-场景对照" tabindex="-1">4.6 场景对照 <a class="header-anchor" href="#_4-6-场景对照" aria-label="Permalink to &quot;4.6 场景对照&quot;">​</a></h3><table tabindex="0"><thead><tr><th>场景</th><th>A（8.8.8.8）</th><th>B（出口自带）</th><th>C（引擎 DNAT）</th><th>D（fake-ip）</th></tr></thead><tbody><tr><td>只做全局出口（现在）</td><td><strong>够用</strong></td><td>没必要</td><td>没必要</td><td>没必要</td></tr><tr><td>出口离线</td><td>服务端撤路由，DNS 回本地，App 仍能用</td><td>DNS 随之失效</td><td>同 B</td><td>同 B</td></tr><tr><td>国内直连（IP 分流）</td><td>8.8.8.8 在国外，仍进隧道，无问题</td><td>overlay 地址，不受排除路由影响</td><td>同 B</td><td>—</td></tr><tr><td>国内域名要走国内 DNS</td><td>做不到</td><td>做不到（要客户端分流）</td><td>可做（引擎里判断域名）</td><td><strong>能做</strong></td></tr><tr><td><code>*.lattice</code> 短名</td><td>不可用</td><td>可用</td><td>可用</td><td>可用</td></tr><tr><td>依赖第三方解析器</td><td>是（8.8.8.8）</td><td>否</td><td>可选</td><td>否</td></tr><tr><td>客户端改动</td><td>最小</td><td>需要传出口地址</td><td>大</td><td>最大</td></tr><tr><td>出口改动</td><td>无</td><td>加转发器</td><td>无</td><td>无</td></tr></tbody></table><h3 id="_4-7-结论" tabindex="-1">4.7 结论 <a class="header-anchor" href="#_4-7-结论" aria-label="Permalink to &quot;4.7 结论&quot;">​</a></h3><ul><li><strong>现阶段用 A</strong>，最简单，已在手机上验证。</li><li><strong>不要为了&quot;出口有 DNS&quot;而做 B</strong>：出口上没有 DNS 服务是正常的，A 根本不需要它。</li><li>B 值得做的时机：出口访问 <code>8.8.8.8</code> 变慢或被限、想在出口做 DNS 日志/缓存、需要在出口模式下保留 <code>*.lattice</code>。</li></ul><h2 id="_5-国内直连、国外走隧道-能做吗" tabindex="-1">5. &quot;国内直连、国外走隧道&quot;能做吗 <a class="header-anchor" href="#_5-国内直连、国外走隧道-能做吗" aria-label="Permalink to &quot;5. &quot;国内直连、国外走隧道&quot;能做吗&quot;">​</a></h2><p>能，分两种：</p><h3 id="_5-1-按-ip-分流-推荐先做" tabindex="-1">5.1 按 IP 分流（推荐先做） <a class="header-anchor" href="#_5-1-按-ip-分流-推荐先做" aria-label="Permalink to &quot;5.1 按 IP 分流（推荐先做）&quot;">​</a></h3><p>全局路由照装 <code>0.0.0.0/0</code>，再把<strong>中国大陆 IP 段</strong>放进 <code>excludedRoutes</code>，这些地址走物理网络直连。这是 WireGuard 客户端做&quot;绕过大陆&quot;的标准做法。</p><ul><li>做法：内置一份大陆 IPv4 段（合并后约几千条），App 加&quot;国内直连&quot;开关，Mac/iOS 的 NE 设置里生效。</li><li>优点：走内核路由，国内流量完全不经隧道，没有额外性能开销，不碰引擎。</li><li>局限： <ul><li><strong>只认 IP，不认域名</strong>，不能写&quot;google.com 走代理、baidu.com 直连&quot;；</li><li>几千条排除路由在 iOS NE 上是否稳定，<strong>需要实测</strong>【推断】；</li><li>国内域名仍被 <code>8.8.8.8</code> 解析（经香港），可能返回境外节点，访问变慢；要解决需要 DNS 也按域名分流；</li><li>列表需要定期更新。</li></ul></li></ul><h3 id="_5-2-按域名-规则分流-真正的-clash-体验" tabindex="-1">5.2 按域名/规则分流（真正的 Clash 体验） <a class="header-anchor" href="#_5-2-按域名-规则分流-真正的-clash-体验" aria-label="Permalink to &quot;5.2 按域名/规则分流（真正的 Clash 体验）&quot;">​</a></h3><p>必须在用户态还原出 TCP/UDP 连接，才知道每个连接要访问的域名。这需要内置协议栈（gVisor），是另一条线，不是加个开关。</p><h2 id="_6-与-clash、tailscale-的对比" tabindex="-1">6. 与 Clash、Tailscale 的对比 <a class="header-anchor" href="#_6-与-clash、tailscale-的对比" aria-label="Permalink to &quot;6. 与 Clash、Tailscale 的对比&quot;">​</a></h2><table tabindex="0"><thead><tr><th></th><th>Clash</th><th>本项目（WireGuard 出口）</th><th>Tailscale（出口节点）</th></tr></thead><tbody><tr><td>工作层</td><td>应用层：还原连接后代理</td><td>网络层：搬运 IP 包</td><td>网络层：搬运 IP 包</td></tr><tr><td>认识域名</td><td>是</td><td>否</td><td>否（DNS 单独处理）</td></tr><tr><td>系统代理</td><td>系统代理模式改系统代理设置；TUN 模式不改</td><td>不改，用虚拟网卡 + 路由</td><td>不改</td></tr><tr><td>DNS</td><td>fake-ip / 远端解析</td><td>系统 DNS 指向解析器，靠路由进隧道</td><td>自带 DNS 转发器，出口模式下转给出口</td></tr><tr><td>防回环</td><td>内部处理</td><td>Mac 绑物理网卡；iOS 未做</td><td>绑物理网卡</td></tr><tr><td>IPv6</td><td>支持</td><td>未接管</td><td>一并接管</td></tr></tbody></table><p>说明：Clash 的部分来自你机器上的实际现象（<code>198.18.0.x</code> 是 Clash 的 fake-ip；<code>scutil --proxy</code> 显示系统代理关、说明你用 TUN 模式）。Tailscale 一栏是我对它设计的了解，<strong>没有逐项核实</strong>【推断】。</p><p><strong>结论：本项目的架构方向和 Tailscale/WireGuard 是同一类，不是 Clash 那一类。</strong> 出问题的是周边实现，不是方向。</p><h2 id="_7-我们踩过的坑-症状-→-原因-→-修复" tabindex="-1">7. 我们踩过的坑（症状 → 原因 → 修复） <a class="header-anchor" href="#_7-我们踩过的坑-症状-→-原因-→-修复" aria-label="Permalink to &quot;7. 我们踩过的坑（症状 → 原因 → 修复）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>#</th><th>症状</th><th>原因</th><th>修复</th></tr></thead><tbody><tr><td>1</td><td>选了出口整机断网，<code>utun5</code> 的 <code>Ipkts=0</code></td><td>交付循环只在攒够 64 个包时才 <code>writePackets</code>，单个回包永远卡住</td><td>非阻塞取空后立刻 flush（<code>bd151981</code>）</td></tr><tr><td>2</td><td>google.com 被解析成假的 Facebook IP</td><td><code>matchDomains=nil</code> 没让隧道 DNS 成为默认解析器，域名走了运营商 DNS</td><td><code>matchDomains=[&quot;&quot;]</code>，系统 DNS 设为公共解析器（<code>bd151981</code>）</td></tr><tr><td>3</td><td>隧道连上几秒就掉线，每秒几千个包</td><td>引擎拦截 DNS 只看端口 53、不看目的地址，把自己转发的上游查询又拦回来，无限循环</td><td>只拦截发往 <code>10.96.0.1</code> 的查询，附回归测试（<code>bd151981</code>）</td></tr><tr><td>4</td><td>App 首页报 <code>LatticeAPIError</code></td><td>控制面把未登录返回成 <strong>HTTP 200 + 响应体 <code>{&quot;code&quot;:401}</code></strong>，App 只处理 HTTP 401，静默重新登录永远不触发</td><td>响应体 <code>code==401</code> 也当未登录（<code>bd151981</code>）</td></tr><tr><td>5</td><td>出口离线时消费端整机断网</td><td>服务端仍给消费端下发 <code>0.0.0.0/0</code></td><td>出口离线就不下发它的宣告路由（<code>6b41ebd3</code>）</td></tr><tr><td>6</td><td>出口重启后消费端长时间停在无出口状态</td><td>在线状态变化没有通知消费端</td><td>出口上下线时推送 netmap 更新（<code>7616c357</code>）</td></tr><tr><td>7</td><td>手机隧道起不来：<code>public key mismatch</code></td><td>手机的设备密钥丢失，服务端拒绝&quot;换密钥&quot;</td><td>删除旧 peer，重新入网（<strong>这次是排查过程中误删了设备容器导致，属于操作失误</strong>）</td></tr><tr><td>8</td><td>手机引擎日志 190 MB</td><td>debug 级别 + 追加打开，从不清理</td><td>启动时超过 5 MB 就清空（<code>bd151981</code>）</td></tr><tr><td>9</td><td>Mac 上开全局后整机断网</td><td><strong>Clash TUN 和 Lattice 的全局路由同时存在，互相捕获对方的流量，形成环路</strong></td><td>未修复，测试时必须先关掉 Clash TUN</td></tr></tbody></table><h2 id="_8-当前状态与路线" tabindex="-1">8. 当前状态与路线 <a class="header-anchor" href="#_8-当前状态与路线" aria-label="Permalink to &quot;8. 当前状态与路线&quot;">​</a></h2><p><strong>已验证</strong>（iPhone）：全局出口 + 干净的域名解析 + Chrome 访问 Google。</p><p><strong>未验证</strong>：</p><ul><li>Mac 上的全局出口（需要先关 Clash TUN，测试时会影响本机网络）；</li><li>方案 A 的 UDP 53 是否确实进入隧道（需要在没缓存的域名上抓包确认）。</li></ul><p><strong>建议路线</strong>：</p><ol><li>继续在手机上使用方案 A，观察稳定性；</li><li>给 iOS 版补上「socket 绑定物理网卡」，与 Mac 对齐；</li><li>补 IPv6 接管，避免绕过出口；</li><li>做&quot;国内直连&quot;的 IP 分流（5.1）；</li><li>之后再评估方案 B（出口自带 DNS）或 gVisor 路线（5.2）。</li></ol><h2 id="_9-术语表" tabindex="-1">9. 术语表 <a class="header-anchor" href="#_9-术语表" aria-label="Permalink to &quot;9. 术语表&quot;">​</a></h2><table tabindex="0"><thead><tr><th>术语</th><th>含义</th></tr></thead><tbody><tr><td>utun</td><td>macOS/iOS 的虚拟网卡，隧道 App 从这里读写 IP 包</td></tr><tr><td>NE / Network Extension</td><td>苹果提供的 VPN 扩展框架，隧道跑在这个扩展进程里</td></tr><tr><td><code>includedRoutes</code> / <code>excludedRoutes</code></td><td>NE 设置里&quot;哪些地址进隧道 / 哪些不进&quot;</td></tr><tr><td><code>matchDomains</code></td><td>隧道 DNS 负责解析哪些域名；<code>[&quot;&quot;]</code> 表示全部</td></tr><tr><td>overlay</td><td>隧道内部的虚拟网络，这里是 <code>10.96.0.0/24</code></td></tr><tr><td>NAT / MASQUERADE</td><td>出口把内层包的源地址改成自己的公网 IP，回程再改回来</td></tr><tr><td>ICE</td><td>两端协商直连 UDP 路径的机制；失败时走 relay 中继</td></tr><tr><td>DNS 污染</td><td>运营商/网络对特定域名返回假地址</td></tr><tr><td>fake-ip</td><td>Clash 的做法：DNS 先返回假地址，连接时再反查域名</td></tr><tr><td>包风暴</td><td>包被反复捕获、重发，每秒成千上万个，把隧道挤死</td></tr></tbody></table><h2 id="_10-验证方法" tabindex="-1">10. 验证方法 <a class="header-anchor" href="#_10-验证方法" aria-label="Permalink to &quot;10. 验证方法&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 手机是否走了出口（在出口机器上）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tcpdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -ni</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wf0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -nn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;host &lt;手机 overlay 地址&gt;&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 手机的 DNS 包是否进了隧道（应看到 源=手机overlay 目的=8.8.8.8:53）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tcpdump</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -ni</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> wf0</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -nn</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;host &lt;手机 overlay 地址&gt; and udp port 53&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Mac 上：隧道网卡是否在收包（Ipkts 应持续增长，为 0 说明下行没交付）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">netstat</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -I</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> utunN</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -bn</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Mac 上：某个地址走哪张网卡</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">route</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 8.8.8.8</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># Mac 上：系统当前用的 DNS（出口模式下应是 8.8.8.8 / 1.1.1.1）</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">scutil</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --dns</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> head</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -20</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;"># 出口机器上：转发和 NAT 规则</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">cat</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /proc/sys/net/ipv4/ip_forward</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">iptables</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -t</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> nat</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -S</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POSTROUTING</span></span></code></pre></div>`,75)])])}const u=a(e,[["render",i]]);export{g as __pageData,u as default};
