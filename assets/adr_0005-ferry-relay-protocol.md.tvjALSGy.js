import{_ as t,o as a,c as o,a0 as r}from"./chunks/framework.DoRPXVp_.js";const u=JSON.parse('{"title":"ADR-0005: FERRY — the Lattice relay service and the FERRY/1 protocol","description":"","frontmatter":{},"headers":[],"relativePath":"adr/0005-ferry-relay-protocol.md","filePath":"adr/0005-ferry-relay-protocol.md"}'),i={name:"adr/0005-ferry-relay-protocol.md"};function n(s,e,d,l,c,h){return a(),o("div",null,[...e[0]||(e[0]=[r(`<h1 id="adr-0005-ferry-—-the-lattice-relay-service-and-the-ferry-1-protocol" tabindex="-1">ADR-0005: FERRY — the Lattice relay service and the FERRY/1 protocol <a class="header-anchor" href="#adr-0005-ferry-—-the-lattice-relay-service-and-the-ferry-1-protocol" aria-label="Permalink to &quot;ADR-0005: FERRY — the Lattice relay service and the FERRY/1 protocol&quot;">​</a></h1><ul><li><strong>Status</strong>: Proposed</li><li><strong>Date</strong>: 2026-09-19</li><li><strong>Related</strong>: ADR-0004 (LRP per-peer authentication), ADR-0003 (client-side keygen)</li><li><strong>Supersedes</strong> (when accepted): the wire format and naming of LRP (&quot;bolt&quot;, &quot;lrp&quot;, &quot;wrrp&quot;, &quot;lrper&quot; are all names of this one service today)</li></ul><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to &quot;Summary&quot;">​</a></h2><p>When two peers cannot reach each other directly (ICE fails: symmetric NAT, UDP blocked, a container behind a bridge), Lattice carries their WireGuard packets through a relay. That relay exists, but live testing on 2026-09-19 showed it had <strong>never actually carried a packet</strong>, and a review of the wire protocol found several design problems that make it unsafe to leave as is.</p><p>This ADR (1) names the service <strong>FERRY</strong> (in the role Tailscale&#39;s DERP plays), (2) records what we found, (3) specifies <strong>FERRY/1</strong>, a cleaned-up wire protocol, and (4) plans a compatible migration from LRP.</p><p>The name: a ferry is what you take when there is no bridge. Peers use the direct path when it exists and are carried across only when it does not; when the bridge is back they go back to it.</p><h2 id="context" tabindex="-1">Context <a class="header-anchor" href="#context" aria-label="Permalink to &quot;Context&quot;">​</a></h2><h3 id="what-the-relay-is-today" tabindex="-1">What the relay is today <a class="header-anchor" href="#what-the-relay-is-today" aria-label="Permalink to &quot;What the relay is today&quot;">​</a></h3><p>Facts, from the code as of commit <code>e28eb9bb</code>:</p><ul><li>One service, several names. <code>lrper</code> (<code>cmd/lrper</code>) is the standalone binary; <code>latticed --standalone</code> embeds the same server in process. Packages, config keys and log lines say <code>lrp</code>, <code>bolt</code> or <code>wrrp</code> interchangeably.</li><li>Two transports. <strong>TCP</strong>: <code>GET /lrp/v1/upgrade</code> with <code>Upgrade: lrp</code> (<code>bolt</code> is also accepted), then an opaque frame stream on the hijacked connection. <strong>QUIC</strong>: ALPN <code>lrp</code>; a control stream carries Register, auth and keepalive, and <code>Forward</code>/<code>Probe</code> frames travel as QUIC datagrams.</li><li>A frame is a 12-byte little-endian header (<code>Seq u16 | PayloadLen u32 | Cmd u8 | ToID u32 | Reserved u8</code>) plus payload. Commands: Register, Forward, KeepAlive, Probe, AuthChallenge, AuthResponse.</li><li>A peer is identified by a 32-bit ID: bytes 4..8 of its WireGuard public key (<code>peerIDFromPublicKey</code>).</li><li><code>Forward</code> carries a WireGuard packet (already encrypted end to end). <code>Probe</code> carries a signaling packet, so the relay can also stand in for NATS.</li><li>Authentication: a shared token in the Register payload, optionally followed by the ADR-0004 X25519 challenge-response. <code>lrp-require-peer-auth</code> defaults to <code>false</code>.</li><li>Client behaviour: a keepalive every 20 s (TCP), reconnect with 1 s to 30 s exponential backoff, QUIC idle timeout 90 s with a 25 s keepalive period.</li><li>The control plane advertises the relay to agents through <code>relay-advertise-url</code>, delivered in the registration response and netmap as <code>LrpUrl</code>.</li><li>The transport race is in <code>Probe.discover</code>: ICE and LRP are dialled concurrently, and if LRP wins, an ICE success within the same call upgrades the connection.</li></ul><h3 id="what-live-testing-showed-2026-09-19" tabindex="-1">What live testing showed (2026-09-19) <a class="header-anchor" href="#what-live-testing-showed-2026-09-19" aria-label="Permalink to &quot;What live testing showed (2026-09-19)&quot;">​</a></h3><p>Testing a Mac, a cloud container and a phone against a cloud control plane found that the relay path had never worked end to end. Each item below was a separate defect, and each hid the next:</p><table tabindex="0"><thead><tr><th>#</th><th>Defect</th><th>Fix</th></tr></thead><tbody><tr><td>1</td><td>The registration response carried no relay URL, so no agent ever created a relay client (server saw 0 sessions, 0 rejections).</td><td><code>501903a5</code></td></tr><tr><td>2</td><td>The relay token contains <code>+</code> (base64). <code>splitURLToken</code> used <code>url.ParseQuery</code>, which turns <code>+</code> into a space, so a correctly configured token was always rejected.</td><td><code>501903a5</code></td></tr><tr><td>3</td><td>The agent created the relay client but never set <code>EnableLrp</code>, which gates both the ICE/LRP race and the bind&#39;s relay receive path.</td><td><code>501903a5</code></td></tr><tr><td>4</td><td>A <code>relay-url</code> written from server defaults (<code>:6266</code>) was treated as an operator override, so the agent dialled localhost.</td><td><code>21dadf0d</code></td></tr><tr><td>5</td><td>The server forwarded a sender&#39;s frame untouched, so the header still held the <strong>receiver&#39;s</strong> own ID. WireGuard&#39;s roaming logic then re-pointed the peer at the node itself. The relay was selected in about a second, but no handshake could ever complete over it (<code>lrp-ready</code>, <code>Handshake: never</code>, probe restarted every 16 s).</td><td><code>129e5357</code></td></tr><tr><td>6</td><td>The cloud security group did not open the relay port, and a container cannot reach its own host&#39;s public IP on it.</td><td>operational</td></tr></tbody></table><p>Separately, the failover around the relay was slow and one-way: a silently dead direct path took about 3 minutes to notice (bounded by the 180 s handshake-age rule), and once on the relay a probe never returned to the direct path (<code>e28eb9bb</code> addresses both).</p><p>Defect 5 is the important one. It is not a coding slip so much as a symptom of the protocol: see finding F3.</p><h3 id="protocol-review-findings" tabindex="-1">Protocol review findings <a class="header-anchor" href="#protocol-review-findings" aria-label="Permalink to &quot;Protocol review findings&quot;">​</a></h3><p>Severity is our judgement. &quot;Verified&quot; means we read the code that does it.</p><table tabindex="0"><thead><tr><th>ID</th><th>Sev</th><th>Finding</th><th>Status</th></tr></thead><tbody><tr><td>F1</td><td>High</td><td><strong>Peer authentication is off by default.</strong> With <code>lrp-require-peer-auth=false</code> the session is inserted before the challenge completes, and <code>SessionManager.Register</code> replaces any existing session for the ID unconditionally. A holder of the shared token can displace a verified peer without proving anything.</td><td>Verified</td></tr><tr><td>F2</td><td>High</td><td><strong>The identity binding is 32 bits.</strong> The claimed ID is compared with 4 bytes of the public key. A keypair whose public key matches those 4 bytes costs about 2^32 X25519 key generations, so an attacker can pass both the prefix check and the challenge for a chosen victim ID. Honest collisions also grow as N²/2^33 (about 1% at 10,000 peers). ADR-0004 reasoned about 8 bytes; the implementation uses 4.</td><td>Verified</td></tr><tr><td>F3</td><td>Med</td><td><strong><code>ToID</code> means three different things</strong>: the client&#39;s own ID in Register, the destination when a client sends, and (before defect 5&#39;s fix) the receiver&#39;s own ID when a frame is delivered. Receivers derive the peer&#39;s identity from it, so any relay that forgets to rewrite it silently breaks routing.</td><td>Verified</td></tr><tr><td>F4</td><td>Med</td><td><strong>No backpressure on the relay&#39;s forward path.</strong> <code>Relay()</code> writes to the destination&#39;s connection synchronously from the <em>sender&#39;s</em> read loop, with no write deadline and no per-session queue. A stalled receiver can block every sender addressing it, including the senders&#39; own keepalive reads.</td><td>Read from code; not load-tested</td></tr><tr><td>F5</td><td>Med</td><td><strong>The control channel is plaintext.</strong> The TCP client dials plain HTTP; the shared token travels in clear. The QUIC client sets <code>InsecureSkipVerify: true</code>, so a man in the middle can read the token and strip traffic. ADR-0004 lists the QUIC part as a known follow-up. Advertising the token-bearing URL to every agent (defect 1&#39;s fix) widens what a leak exposes.</td><td>Verified</td></tr><tr><td>F6</td><td>Low</td><td><strong>One relay, no selection.</strong> The netmap names a single relay for everyone: a single point of failure and no proximity choice.</td><td>Verified</td></tr><tr><td>F7</td><td>Low</td><td><strong>Format inconsistencies.</strong> Header is little-endian while IDs are big-endian elsewhere; <code>Seq</code> is written but never read; <code>Reserved</code> has no version semantics; five names for one service.</td><td>Verified</td></tr><tr><td>F8</td><td>Low</td><td><strong>No server-driven liveness.</strong> The relay does not ping; a half-open client connection is only discovered on write failure or a 30 s read deadline.</td><td>Verified (TCP)</td></tr><tr><td>F9</td><td>?</td><td><strong>QUIC datagram size.</strong> WireGuard packets on our overlay (MTU 1280 on the Mac tunnel) plus a header can exceed a QUIC datagram. Oversized sends would be dropped.</td><td><strong>Unverified</strong>, needs a test</td></tr></tbody></table><p>ADR-0004 also differs from the code in places (Register payload contents, AuthResponse size, 64-bit vs 32-bit ID). This ADR treats the code as the truth and corrects the record.</p><h2 id="goals-and-non-goals" tabindex="-1">Goals and non-goals <a class="header-anchor" href="#goals-and-non-goals" aria-label="Permalink to &quot;Goals and non-goals&quot;">​</a></h2><p>Goals:</p><ol><li>A single, documented, versioned wire protocol with one meaning per field.</li><li>Identity that cannot be spoofed by holders of the shared token or by brute-forcing a short ID prefix.</li><li>A relay that cannot be stalled by one slow receiver.</li><li>Confidentiality of the control channel (token, metadata).</li><li>A migration in which old and new agents and relays interoperate.</li><li>Enough observability to answer &quot;is this peer relayed, and is the relay healthy&quot; without reading logs.</li></ol><p>Non-goals (this ADR):</p><ul><li>Relay-to-relay meshing and a &quot;home relay&quot; model like DERP&#39;s (see <em>Future work</em>).</li><li>Hiding metadata from the relay operator.</li><li>Replacing NATS for signaling. <code>Probe</code> stays as a fallback.</li><li>Changing what is relayed. Payloads stay opaque WireGuard packets.</li></ul><h2 id="decision" tabindex="-1">Decision <a class="header-anchor" href="#decision" aria-label="Permalink to &quot;Decision&quot;">​</a></h2><h3 id="_1-name-and-identifiers" tabindex="-1">1. Name and identifiers <a class="header-anchor" href="#_1-name-and-identifiers" aria-label="Permalink to &quot;1. Name and identifiers&quot;">​</a></h3><p>The service is <strong>FERRY</strong>; the protocol is <strong>FERRY/1</strong>.</p><table tabindex="0"><thead><tr><th>Today</th><th>After</th></tr></thead><tbody><tr><td><code>lrper</code> binary</td><td><code>ferryd</code></td></tr><tr><td><code>LRP relay server</code> / <code>lrp</code> / <code>bolt</code> / <code>wrrp</code></td><td><code>FERRY</code></td></tr><tr><td><code>Upgrade: lrp</code>, <code>/lrp/v1/upgrade</code></td><td><code>Upgrade: ferry/1</code>, <code>/ferry/v1/upgrade</code></td></tr><tr><td>QUIC ALPN <code>lrp</code></td><td><code>ferry/1</code></td></tr><tr><td>config <code>relay-url</code>, <code>relay-quic-url</code>, <code>relay-advertise-url</code>, <code>lrp-auth-token</code>, <code>lrp-require-peer-auth</code>, <code>enable-lrp</code></td><td><code>ferry-url</code>, <code>ferry-quic-url</code>, <code>ferry-advertise-url</code>, <code>ferry-token</code>, <code>ferry-require-peer-auth</code>, <code>enable-ferry</code></td></tr><tr><td>netmap field <code>LrpUrl</code></td><td><code>FerryUrl</code></td></tr><tr><td>transport state strings <code>lrp-ready</code></td><td>see <em>Open questions</em></td></tr></tbody></table><p>Every old name is accepted as an alias for the transition (see Migration).</p><h3 id="_2-ferry-1-wire-format" tabindex="-1">2. FERRY/1 wire format <a class="header-anchor" href="#_2-ferry-1-wire-format" aria-label="Permalink to &quot;2. FERRY/1 wire format&quot;">​</a></h3><p>All integers are <strong>big-endian</strong>. A frame is a 24-byte header followed by <code>PayloadLen</code> bytes.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span> 0        1        2        4                 8                      16                     24</span></span>
<span class="line"><span> +--------+--------+--------+-----------------+----------------------+----------------------+</span></span>
<span class="line"><span> |  Ver   |  Cmd   | Flags  |   PayloadLen    |        SrcID         |        DstID         |</span></span>
<span class="line"><span> |  u8=1  |   u8   |  u16   |       u32       |         u64          |         u64          |</span></span>
<span class="line"><span> +--------+--------+--------+-----------------+----------------------+----------------------+</span></span></code></pre></div><ul><li><code>Ver</code>: protocol version, 1. Frames with an unknown <code>Ver</code> close the session.</li><li><code>Flags</code>: reserved, must be 0 in v1; receivers ignore unknown bits.</li><li><code>SrcID</code> / <code>DstID</code> are the full 64-bit <code>PeerID</code> (the first 8 bytes of the WireGuard public key, <code>infra.FromKey</code>), the same value already used for NATS subjects and the fake endpoint address.</li><li><strong><code>SrcID</code> is set by the relay, never trusted from the client.</strong> On <code>Forward</code> and <code>Probe</code> the relay overwrites <code>SrcID</code> with the sender&#39;s authenticated session ID before delivery, so a receiver can rely on it. <code>DstID</code> is set by the sender and is left unchanged. This gives each field exactly one meaning.</li><li><code>Seq</code> is dropped.</li></ul><p>The header grows from 12 to 24 bytes per relayed packet: on a 1400-byte packet that is about 1.7% total header overhead instead of about 0.9%. That is the price of 64-bit IDs and a version byte; it does not affect direct connections.</p><p>Commands:</p><table tabindex="0"><thead><tr><th>Cmd</th><th>Value</th><th>Direction</th><th>Payload</th></tr></thead><tbody><tr><td>Hello</td><td>0x01</td><td>client → relay</td><td>client public key (32 B), capabilities (u32)</td></tr><tr><td>Challenge</td><td>0x02</td><td>relay → client</td><td>ephemeral X25519 public key (32 B), relay capabilities (u32)</td></tr><tr><td>Proof</td><td>0x03</td><td>client → relay</td><td><code>HMAC-SHA256(token, challenge || clientPub)</code> (32 B) || <code>X25519(clientPriv, challenge)</code> (32 B)</td></tr><tr><td>Ready</td><td>0x04</td><td>relay → client</td><td>empty; the session is now usable</td></tr><tr><td>Forward</td><td>0x10</td><td>both</td><td>opaque WireGuard packet</td></tr><tr><td>Probe</td><td>0x11</td><td>both</td><td>signaling packet</td></tr><tr><td>KeepAlive</td><td>0x20</td><td>both</td><td>empty; either side may send, the peer answers with KeepAlive</td></tr><tr><td>Error</td><td>0x7f</td><td>relay → client</td><td>u16 code, then text; sent only after authentication</td></tr></tbody></table><p><code>Hello</code> replaces <code>Register</code>. It carries the <strong>full 32-byte public key</strong>, and the relay derives <code>PeerID</code> from it; the client no longer claims an ID. Because the token now enters an HMAC instead of being sent, the token is no longer disclosed to the relay in clear or to anyone observing an unencrypted leg (TLS still protects everything else; see 4).</p><h3 id="_3-session-lifecycle-and-authentication" tabindex="-1">3. Session lifecycle and authentication <a class="header-anchor" href="#_3-session-lifecycle-and-authentication" aria-label="Permalink to &quot;3. Session lifecycle and authentication&quot;">​</a></h3><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>client                                        ferryd</span></span>
<span class="line"><span>  |  Upgrade: ferry/1  (or QUIC ALPN ferry/1)   |</span></span>
<span class="line"><span>  |--------------------------------------------&gt;|</span></span>
<span class="line"><span>  |  Hello   { pub, caps }                      |</span></span>
<span class="line"><span>  |--------------------------------------------&gt;|   no session exists yet</span></span>
<span class="line"><span>  |  Challenge { eph_pub, caps }                |</span></span>
<span class="line"><span>  |&lt;--------------------------------------------|</span></span>
<span class="line"><span>  |  Proof   { HMAC(token, eph||pub), DH }      |</span></span>
<span class="line"><span>  |--------------------------------------------&gt;|   verify token, DH, pub prefix == PeerID</span></span>
<span class="line"><span>  |  Ready                                      |</span></span>
<span class="line"><span>  |&lt;--------------------------------------------|   session inserted, now routable</span></span>
<span class="line"><span>  |  Forward / Probe / KeepAlive ...            |</span></span></code></pre></div><p>Rules:</p><ol><li><strong>A session is created only after <code>Proof</code> verifies.</strong> No lenient mode in v1. An unauthenticated connection never occupies an ID and never receives traffic. There is a 10 s deadline from connect to <code>Ready</code>.</li><li><strong>Takeover.</strong> A verified session for the same public key replaces the previous one (this is what a reconnect is). A verified session for a <em>different</em> public key that maps to an occupied <code>PeerID</code> is refused (<code>Error: id-collision</code>) while the incumbent is alive. This closes F1 and makes a 64-bit prefix collision an availability problem for at most one of two peers rather than a hijack.</li><li><strong>Liveness.</strong> Both sides may send <code>KeepAlive</code>; the relay sends one if a session is idle for 15 s and closes it if nothing arrives for 45 s. This addresses F8 and gives the relay a way to notice half-open TCP connections.</li><li><strong>Version negotiation.</strong> The transport chooses the codec: TCP by the <code>Upgrade</code> token (<code>ferry/1</code>, else legacy <code>lrp</code>/<code>bolt</code>), QUIC by ALPN. There is no sniffing of frame bytes, which would be ambiguous because a legacy header begins with an arbitrary <code>Seq</code>.</li></ol><h3 id="_4-confidentiality" tabindex="-1">4. Confidentiality <a class="header-anchor" href="#_4-confidentiality" aria-label="Permalink to &quot;4. Confidentiality&quot;">​</a></h3><ul><li>TCP mode uses TLS (HTTPS upgrade) by default. Plain HTTP is available only with an explicit <code>ferry-insecure</code> setting, for loopback and tests.</li><li>QUIC already runs TLS 1.3; <code>InsecureSkipVerify</code> is removed.</li><li><strong>Pinning.</strong> Because a relay is often self-hosted with a self-signed certificate, the control plane advertises the relay&#39;s certificate fingerprint next to its address: <code>ferry://host:6266#sha256=&lt;hex&gt;</code>. Agents pin it. This avoids a public CA requirement and removes the MITM in F5.</li><li>The token stays a membership credential, not an identity: identity comes from the DH proof, membership from the HMAC.</li></ul><h3 id="_5-data-path-semantics" tabindex="-1">5. Data path semantics <a class="header-anchor" href="#_5-data-path-semantics" aria-label="Permalink to &quot;5. Data path semantics&quot;">​</a></h3><p><code>Forward</code> is <strong>best effort, unordered, unacknowledged</strong>, exactly like the UDP it carries. The relay must never let one receiver slow another sender.</p><ul><li>Each session has a bounded outbound queue (default 256 frames or 512 KiB, whichever is smaller). <code>Relay()</code> enqueues and returns; a per-session writer drains the queue with a 2 s write deadline.</li><li>When the queue is full, <strong>drop the new frame</strong> and count it. Never block the sender, never buffer without bound.</li><li>A writer that misses its write deadline closes the session; the client reconnects with its existing backoff.</li><li>Oversize frames (<code>PayloadLen</code> above 64 KiB) close the session before any allocation, as today.</li><li>Datagram-size handling on QUIC is decided by test F9; if oversized packets are dropped, either the relay advertises a maximum forward size in <code>Challenge</code> so the agent lowers the tunnel MTU on relayed paths, or <code>Forward</code> falls back to a stream for large frames.</li></ul><h3 id="_6-observability" tabindex="-1">6. Observability <a class="header-anchor" href="#_6-observability" aria-label="Permalink to &quot;6. Observability&quot;">​</a></h3><p>The relay package has no metrics today. Add these, with the <code>ferry_</code> prefix, using the same metrics library the agent already uses:</p><ul><li><code>ferry_sessions</code> (gauge, by transport tcp/quic)</li><li><code>ferry_frames_total{cmd,result=forwarded|no_route|dropped_full|dropped_oversize}</code></li><li><code>ferry_bytes_total{direction}</code></li><li><code>ferry_auth_failures_total{reason}</code></li><li><code>ferry_session_writer_stalls_total</code></li></ul><p>Agent side, surfaced through the existing <code>lattice status</code> transport line: whether a peer is relayed, and which relay carries it. The <code>Transport:</code> line introduced with the status enhancement already prints <code>lrp-ready (relayed)</code>; FERRY adds the relay address and the relay RTT to it.</p><h2 id="migration-and-compatibility" tabindex="-1">Migration and compatibility <a class="header-anchor" href="#migration-and-compatibility" aria-label="Permalink to &quot;Migration and compatibility&quot;">​</a></h2><p>The wire change is breaking, so it ships in phases. At every phase an old agent works against a new relay and the reverse.</p><table tabindex="0"><thead><tr><th>Phase</th><th>Relay (<code>ferryd</code>)</th><th>Agent</th><th>Notes</th></tr></thead><tbody><tr><td>P0 (done)</td><td>Fix defects 1 to 6</td><td>Same</td><td>Already released as <code>501903a5</code>, <code>21dadf0d</code>, <code>129e5357</code></td></tr><tr><td>P1</td><td>Dual-stack: serves FERRY/1 and legacy LRP on the same listener (by <code>Upgrade</code> token / ALPN). Non-blocking forward queue and write deadline apply to <strong>both</strong> codecs. Legacy sessions cannot replace verified ones.</td><td>Unchanged</td><td>Fixes F1 (partly), F4 immediately, with no agent change</td></tr><tr><td>P2</td><td>Same</td><td>Speaks FERRY/1 when the relay advertises it (<code>FerryUrl</code> present), else legacy</td><td>Netmap carries both <code>LrpUrl</code> and <code>FerryUrl</code></td></tr><tr><td>P3</td><td><code>ferry-legacy=false</code> becomes the default</td><td>Same</td><td>Legacy support is removed one release after telemetry shows no legacy sessions</td></tr><tr><td>P4</td><td>Rename binary, config keys, metrics, log fields</td><td>Same</td><td>Old names stay as aliases for two releases, with a deprecation log line</td></tr></tbody></table><p>Two things stay stable across the rename because outside consumers read them: the transport state strings used by the Apple client (<code>ConnectionStates()</code>), and the <code>lrp-ready</code> text in <code>lattice status</code>. Both change only with a deprecation cycle, see <em>Open questions</em>.</p><h2 id="testing-plan" tabindex="-1">Testing plan <a class="header-anchor" href="#testing-plan" aria-label="Permalink to &quot;Testing plan&quot;">​</a></h2><ul><li>Unit: frame encode/decode round trip and rejection of unknown <code>Ver</code>; <code>SrcID</code> is overwritten on forward whatever the client wrote; HMAC and DH proof happy path and each failure (bad token, wrong DH, prefix mismatch, low-order point); takeover rules (same key replaces, different key refused).</li><li>Integration on loopback (extends <code>integration_auth_test.go</code>): two clients, frame from A to B arrives with <code>SrcID = A</code> (the regression test for defect 5 already exists in the LRP codec and moves over).</li><li>Backpressure: a receiver that never reads must not delay delivery between two other peers, the queue depth is bounded, and <code>dropped_full</code> increases.</li><li>Compatibility: legacy client against the P1 relay; FERRY/1 client against a legacy-only relay (falls back).</li><li>Fuzz: the header and Hello/Proof parsers.</li><li>F9: measure the largest WireGuard packet that survives a relayed round trip over QUIC datagrams at the tunnel MTU we ship.</li><li>End to end on the cloud deployment: the two experiments that exposed defect 5 (block the direct UDP path in the <code>raw</code> table so conntrack cannot short-circuit it, then confirm recovery over the relay, then confirm the upgrade back to direct).</li></ul><h2 id="alternatives-considered" tabindex="-1">Alternatives considered <a class="header-anchor" href="#alternatives-considered" aria-label="Permalink to &quot;Alternatives considered&quot;">​</a></h2><ul><li><strong>Keep LRP and fix defects in place.</strong> Fixes F1, F4 cheaply (and P1 does exactly that), but leaves F2, F3, F5 and F7, which are wire-format problems.</li><li><strong>Adopt Tailscale&#39;s DERP protocol, or embed its <code>derper</code>.</strong> DERP addresses frames by the full 32-byte public key, has a home-relay model and relay meshing, and is well proven. As we understand it, it is designed around its own coordination server&#39;s node keys and region map; adopting it means taking on that model and its dependencies for a small fraction of what we use. FERRY/1 borrows the good parts (server-stamped source, best-effort datagram semantics, HTTP-upgrade transport) without the mesh. We have not evaluated <code>derper</code> in depth, so this is a judgement to revisit if we later want relay meshing.</li><li><strong>32-byte IDs in every frame.</strong> Removes the prefix question entirely but costs 48 extra bytes per packet. 64 bits plus rejection of a colliding <em>different</em> key at registration (rule 2) gives the same practical guarantee for the price of 12 bytes.</li><li><strong>QUIC only.</strong> Simplifies the protocol, but QUIC is UDP, and the relay exists for networks where direct UDP fails. The TCP transport must stay.</li><li><strong>Encrypt the relay hop.</strong> Payloads are already WireGuard ciphertext. Hop encryption would only hide metadata from the operator, which is a non-goal.</li></ul><h2 id="security-analysis" tabindex="-1">Security analysis <a class="header-anchor" href="#security-analysis" aria-label="Permalink to &quot;Security analysis&quot;">​</a></h2><p>Closes:</p><ul><li>Displacing a verified peer by a token holder (F1), through the &quot;no session before Proof&quot; and takeover rules.</li><li>ID-prefix brute force (F2): the brute-force cost rises from about 2^32 to about 2^64 key generations, and even a successful match cannot displace a live incumbent.</li><li>Confused-deputy routing (F3): one meaning per field, <code>SrcID</code> only ever set by the relay.</li><li>Token disclosure to an on-path observer and the QUIC MITM (F5).</li><li>A slow receiver stalling unrelated senders (F4).</li></ul><p>Remains open:</p><ul><li>The relay sees metadata: who connects, when, how much.</li><li>A valid member can still flood the relay. Per-session rate limits are future work.</li><li>The token is a shared secret. Losing it lets an attacker use relay capacity (not impersonate a peer); rotation needs a coordinated change.</li><li>Availability under a deliberate 64-bit collision squatting an offline victim&#39;s ID is theoretically possible at about 2^64 cost and is accepted.</li></ul><h2 id="consequences" tabindex="-1">Consequences <a class="header-anchor" href="#consequences" aria-label="Permalink to &quot;Consequences&quot;">​</a></h2><ul><li>Relayed packets carry 12 more bytes of header. Direct paths are unaffected.</li><li>Relay and agent releases must follow the phase table; skipping P1 makes P2 agents talk to a relay that only knows LRP.</li><li>The protocol gains a real version field, so later changes (multi-relay, rate limits) no longer need another flag day.</li><li>One name, <code>FERRY</code>, replaces five. Renaming touches the binary, config keys, metrics and docs, and is staged in P4 so it can lag behind the protocol work.</li></ul><h2 id="future-work" tabindex="-1">Future work <a class="header-anchor" href="#future-work" aria-label="Permalink to &quot;Future work&quot;">​</a></h2><ul><li><strong>Relay descriptors in the netmap</strong>: a list of relays with region and fingerprint instead of one URL, so agents pick the lowest-RTT one.</li><li><strong>Home relay and relay-to-relay forwarding</strong> (the DERP model), once one relay per deployment is no longer enough.</li><li><strong>Per-session rate limits and abuse controls.</strong></li><li><strong>Seamless relay-to-direct upgrade.</strong> Today the upgrade is a full probe restart with backoff because the signaling has no &quot;upgrade only&quot; flag. A flag in the SYN would let the responder run ICE alone while the relay keeps carrying traffic, removing the brief interruption per attempt.</li></ul><h2 id="open-questions" tabindex="-1">Open questions <a class="header-anchor" href="#open-questions" aria-label="Permalink to &quot;Open questions&quot;">​</a></h2><ol><li><strong>Transport state strings.</strong> Rename <code>lrp-ready</code> to <code>relay-ready</code>, or to <code>ferry-ready</code>? The Apple client reads these strings, so the answer decides the deprecation plan.</li><li><strong>Should P1 (backpressure) be released ahead of everything else?</strong> It needs no agent change and removes the only finding that can hurt other peers today.</li><li><strong>TLS on the all-in-one path.</strong> <code>latticed --standalone</code> embeds the relay; do we generate and pin a self-signed certificate automatically, or require the operator to supply one?</li><li><strong>Token rotation.</strong> Accept two tokens for a window, or move to per-workspace relay credentials issued by the control plane at registration?</li><li><strong>F9.</strong> What is the largest relayed packet at the shipped MTU? The answer decides between an MTU advertisement and a stream fallback.</li></ol>`,69)])])}const y=t(i,[["render",n]]);export{u as __pageData,y as default};
